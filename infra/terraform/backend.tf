# Uncomment to configure remote backend for state storage
# This should be uncommented after running `terraform init` locally
# with a local backend, then migrated to GCS

# terraform {
#   backend "gcs" {
#     bucket = "musicai-terraform-state"
#     prefix = "musicai"
#   }
# }
